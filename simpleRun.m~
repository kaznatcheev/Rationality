function [ output_args ] = simpleRun(n_agents,n_epochs)
%The goal of simpleRun is to provide a way to quickly test run the code
%and look at its outputs

%First we make a random graph of degree 3
adjmx = full(createRandRegGraph(n_agents, 3));

subR

end

